import { Routes } from '@angular/router';
import { PaymentsListPage } from '@/features/admin/payments/pages/payments-list-page/payments-list-page';

export const routes: Routes = [{ path: '', component: PaymentsListPage }];
