import { Routes } from '@angular/router';
import { MaintenancePage } from '@/features/public/maintenance/pages/maintenance-page/maintenance-page';

export const routes: Routes = [{ path: '', component: MaintenancePage }];
